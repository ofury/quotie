name: quotie
description: Quotes

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.0

  cupertino_icons: ^0.1.2
  http: ^0.12.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^3.2.2
  build_runner: ^1.7.1

flutter:
  uses-material-design: true
  assets:
   - assets/quotes.json
